# 代码结构总结

## 1. 总体数据流

```text
固定 raw-sample manifest
  -> 融合 RMSNorm 与可合并 Hadamard rotation，保存 fused Base checkpoint
  -> 用 fused Base checkpoint 发现固定 PrefixQuant tokens
  -> 在 prefix + 实际旋转坐标下收集每层十个 site 的统计量
  -> 保存并冻结 Phase / GIF / MTN / common clip state
  -> 对 fused weights 做全参数 ANN fine-tuning
  -> 每个 ANN checkpoint 分别创建 Phase、GIF、MTN conversion descriptor
  -> full-temporal SNN evaluation
```

Vanilla 分支不使用 rotation、prefix 或训练期 replacement，但在原始坐标下生成独立 frozen calibration state，并同样转换为三种 SNN。

## 2. 目录结构

```text
SNN/
├── configs/
│   ├── experiment_matrix.yaml       # 3 个 model-task pair × 4 ANN modes
│   └── deepspeed_zero3.json         # 全参微调 ZeRO-3
├── scripts/
│   ├── _common.py                   # 配置、seed、artifact layout 公共入口
│   ├── materialize_configs.py       # 生成12个 resolved run config
│   ├── prepare_data.py              # 保存 train/validation/test/calibration manifest
│   ├── prepare_rotation.py          # 生成并保存 fused Base checkpoint
│   ├── discover_prefix.py           # 发现并保存固定 prefix token policy
│   ├── calibrate_sites.py            # 收集十个 site 统计并生成 frozen states
│   ├── train_ann.py                 # DeepSpeed ZeRO-3 全参 ANN 微调
│   ├── convert_snn.py               # 创建三种 SNN conversion descriptor
│   ├── evaluate_tldr.py             # ANN/SNN 的 TL;DR ROUGE 评测
│   ├── evaluate_lm_harness.py       # ANN/SNN 的 Tulu downstream 评测
│   └── verify_artifacts.py          # 校验 checkpoint、十个 site 与评测工件
├── snn2/
│   ├── artifacts.py                 # 共享与 run-specific 工件目录
│   ├── config.py                    # 配置解析、四种 ANN mode 与约束检查
│   ├── data.py                      # 固定抽样、chat template、loss mask、collator
│   ├── hadamard.py                  # 随机 Hadamard、H12/H28 与 CUDA FHT 接口
│   ├── rotation.py                  # RMSNorm fusion 与 R1/R2/R4 权重融合
│   ├── prefix.py                    # PrefixQuant outlier-token 统计
│   ├── sites.py                     # 十个 activation replacement site 的统一 topology 定义
│   ├── stats.py                     # site distribution/operator saliency 累计与保存
│   ├── calibration.py               # 静态神经元参数与公共区间初始化
│   ├── neurons.py                   # Phase、static GIF、MTN 与 hard clip
│   ├── controller.py                # collect/train/deploy site 状态机
│   ├── model_integration.py         # 十个 site、R3/R4 online 与 temporal forward
│   ├── modeling.py                  # model/tokenizer/checkpoint 加载
│   ├── training.py                  # task-loss-only 全参微调
│   ├── conversion.py                # frozen-state 校验与 conversion metadata
│   ├── evaluation.py                # ANN/full-temporal SNN 生成及 lm-eval adapter
│   └── logging_utils.py             # `.log`、`.jsonl`、result JSON 与资源统计
├── requirements.txt
├── 环境配置.md
├── 代码结构总结.md
└── 实验执行总结.md
```

## 3. Rotation 与可训练权重

实现采用“可合并 rotation 吸收到权重 + 少量 online rotation”的混合方式：

- 先把 RMSNorm scale 吸收到相邻线性权重；
- `R1` 作用于 residual hidden space，并融合进 embedding、q/k/v、gate/up、o/down 和 lm_head 的相应权重侧；
- `R2` 作用于 value/head 坐标，融合进 v/o projection；
- `R3` 在 RoPE 后对 Q/K 在线计算，利用正交性在 QK dot product 中相消；
- `R4` 在 MLP gate-up product 后在线计算，其逆变换融合进 down projection；
- rotation matrix/policy 固定，但保存到 `fused_base` 的旋转后权重是后续全参数微调的对象。原始 Hugging Face Base 权重只作为源 checkpoint，不被原地修改。

CUDA tensor 优先调用 `Dao-AILab/fast-hadamard-transform`；H12/H28 与纯 Torch FHT 是结构支持及正确性 fallback。

## 4. 十个 activation replacement site

固定映射为：

```text
1:R1, 2:R3, 3:R3, 4:R2, 5:I, 6:R2, 7:R1, 8:I, 9:I, 10:R4
```

Softmax 后、SiLU 后以及 up_proj 后均不增加额外 rotation；新增 Site 9 位于 up_proj 输出后，原 R4 后 MLP product site 顺延为 Site 10。训练期 Phase/GIF replacement 在每个 site 内立即聚合为静态 tensor；层间不携带时间维。deployment 才把时间维作为 batch-major 展开形式跨层保留：embedding 只在第一个 timestep 注入，之后各算子按 timestep 传播，最后只在 lm_head 后做 temporal sum。

## 5. Calibration 工件

每个 `layer_xxx/site_xx_name/` 至少保存：

```text
statistics.pt
statistics_summary.json
phase_state.pt
gif_state.pt
mtn_state.pt
clip_state.pt
calibration_summary.json
```

`statistics.pt` 包含 min/max/absolute-max、sum-absolute、sum-square、operator-aware saliency sum/count。所有 state 都在 Base checkpoint 的 calibration 阶段生成；ANN 微调后禁止重新校准。

GIF 原始 SpikeLLM forward 使用 per-sample-per-token dynamic scale/zero point；本项目明确改为 site-specific static direct-min-max。operator-aware mask 的扩展规则为：

- 线性消费位置使用 `Z * ((Z W^T) W)`；
- site 1、7 汇总多个下游线性分支；
- Q/K 与 Softmax/V 使用 SpikeQuantMatMul 式双操作数 sensitivity，并对第一操作数作对称扩展；
- site 8 与 site 9 分别对应 gate/up 两个 elementwise-product operand，二者均使用 gate² × up² 的 product-aware sensitivity；site 10 使用 R4 后 product → down_proj 的 linear-consumer sensitivity；
- site 5 保存 `[2048]` key-position mask，runtime 截取 `[:L]`，未观测位置固定为 4-bit；其 scale/zero point 仍为单组标量。

上述第一操作数、multi-branch、site 8 和 site 5 可变长度处理是本项目为覆盖十个 site 所作的明确推广，不冒充 SpikeLLM 原文逐字实现。

## 6. 工件共享层级

```text
artifacts/<experiment>/<task>/
├── _shared/seed42/data/                     # 同一任务共享 raw manifest
└── <model>/
    ├── _shared/seed42/
    │   ├── rotated_prefix/rotation/
    │   ├── rotated_prefix/prefix/
    │   ├── rotated_prefix/calibration/
    │   └── vanilla_original/calibration/
    └── <ann_mode>/lr<learning_rate>/seed42/
        ├── config/
        ├── ann/
        │   └── final/              # canonical fine-tuned ANN checkpoint
        ├── snn/{phase,gif,mtn}/
        └── logs/
```

因此 unaware、phase-aware、GIF-aware 共用同一 model-task 的 rotation、prefix 和 frozen calibration；Vanilla 只共用 raw manifest。
