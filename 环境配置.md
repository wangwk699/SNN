# 环境配置

本目录只提供配置文件和命令，不会自动安装环境。建议在 Linux、CUDA GPU 和 Python 3.10 环境中运行；Llama 3 checkpoint 是 gated model，需先在 Hugging Face 接受许可并登录。

## 1. 创建环境

```bash
conda create -n snn2 python=3.10 -y
conda activate snn2
python -m pip install --upgrade pip setuptools wheel
```

先按照机器 CUDA 驱动选择 PyTorch 官方 wheel。下面以 CUDA 12.4 为例：

```bash
pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu124
pip install -r requirements.txt
```

`requirements.txt` 中也固定了 `torch==2.5.1`，已安装相同版本时 pip 不会重复替换。若机器只能使用其他 CUDA wheel，只改变 PyTorch 的 wheel 来源，不要静默改变其余依赖版本。

## 2. 安装 Dao-AILab Fast Hadamard Transform

以下步骤以已经克隆 `https://github.com/wangwk699/SNN.git`、完成第 1 节环境安装为前提，请在 SNN 仓库根目录执行。依赖通过 SSH 单独克隆到 `fast-hadamard-transform/`，不再作为 SNN 的 Git submodule 管理；该目录应加入 SNN 的 `.gitignore`，不提交到 SNN 仓库。访问 GitHub 的 SSH 密钥需事先配置好。

当本地尚无 `fast-hadamard-transform/` 目录时执行：

```bash
cd /home/wangwenkang/SNN  # 按实际 SNN 仓库路径调整

git clone git@github.com:Dao-AILab/fast-hadamard-transform.git fast-hadamard-transform
git -C fast-hadamard-transform checkout e7706faf8d1c3b9f241e36860640ad1dac644ede

python -m pip install -e ./fast-hadamard-transform --no-build-isolation
```

这里通过手动 checkout 固定依赖版本，不依赖 SNN 的子模块记录。如果该目录已经存在且是有效的依赖 Git 仓库，保留本地文件，跳过 `git clone`，确认本地修改后再执行 checkout 和安装；旧子模块需先在 SNN 中取消跟踪并移除对应的 `.gitmodules` 配置。不要直接删除包含本地修改的依赖目录。

`-e` 表示 editable install，Python 会直接使用当前项目 `fast-hadamard-transform/` 目录对应的源码和所编译扩展。仅有该目录并不能保证 CUDA extension 可用，仍必须执行上述 editable install。

安装后必须核对实际 import 来源：

```bash
python - <<'PY'
import fast_hadamard_transform
print(fast_hadamard_transform.__file__)
from fast_hadamard_transform import hadamard_transform
print("fast-hadamard-transform OK")
PY
```

输出路径必须位于：

```text
<SNN_ROOT>/fast-hadamard-transform/fast_hadamard_transform/
```

正式运行行为固定为：

```text
CPU correctness/test path:
    允许 pure Torch implementation

CUDA formal experiment:
    必须使用 fast-hadamard-transform
    import 或 kernel 失败会直接报错
    禁止静默回退到 pure Torch
```

## 3. Hugging Face 权限与缓存

```bash
huggingface-cli login
export HF_HOME=/path/to/large/huggingface/cache
```

需要确认当前账号可以下载：

- `Qwen/Qwen3-1.7B-Base`
- `Qwen/Qwen3-8B-Base`
- `meta-llama/Meta-Llama-3-8B`
- `trl-lib/tldr`
- `allenai/tulu-3-sft-mixture`

```python
from datasets import load_dataset 
tldr_dataset = load_dataset("trl-lib/tldr")
print(tldr_dataset)

tulu_dataset = load_dataset("allenai/tulu-3-sft-mixture")
print(tulu_dataset)

DatasetDict({
    train: Dataset({
        features: ['prompt', 'completion'],
        num_rows: 116722
    })
    validation: Dataset({
        features: ['prompt', 'completion'],
        num_rows: 6447
    })
    test: Dataset({
        features: ['prompt', 'completion'],
        num_rows: 6553
    })
})

DatasetDict({
    train: Dataset({
        features: ['id', 'messages', 'source'],
        num_rows: 939343
    })
})
```

配置矩阵已经固定 model/dataset revision；如某个 gated revision 无法访问，应先处理访问权限，不要改成 Instruct checkpoint。

## 4. 环境自检

```bash
python -c "import torch, transformers, deepspeed, datasets, lm_eval; print(torch.__version__, transformers.__version__, torch.cuda.is_available())"
python - <<'PY'
import fast_hadamard_transform
print(fast_hadamard_transform.__file__)
from fast_hadamard_transform import hadamard_transform
print("fast-hadamard-transform OK")
PY
python -m compileall -q SNN
```

正式运行前用 `nvidia-smi` 检查可见 GPU，并保证同一 model-task pair 的四种 ANN 设置使用相同 GPU 数。训练采用 DeepSpeed ZeRO-3、BF16、eager attention；不得静默启用 FlashAttention 或 gradient checkpointing。

## 5. 固定的外部实现 revision

- RoSTE：`3084bae31641298cded3decde1ec8ae0272d5e65`
- lm-evaluation-harness：`6d2abda4fd171e68a8789330c4149e37c1ca0bda`（v0.4.8）
- fast-hadamard-transform：`e7706faf8d1c3b9f241e36860640ad1dac644ede`

每个阶段的 result JSON 还会记录实际 Python、PyTorch、Transformers、Datasets、TRL、Accelerate 和 DeepSpeed 版本、git commit、wall-clock time 与峰值 CUDA 显存。