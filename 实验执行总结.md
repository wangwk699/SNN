# 实验执行总结

以下命令只说明运行顺序，不代表已经执行实验。所有命令都从项目根目录 `SNN2/` 运行。环境安装见 `code/环境配置.md`。

## 1. 生成12个 ANN 配置

```bash
python code/scripts/materialize_configs.py
```

输出位于 `code/configs/generated/`：

```text
exp1_qwen3_1_7b_tldr__{vanilla,unaware,phase_aware,gif_aware}.yaml
exp1_qwen3_8b_tldr__{vanilla,unaware,phase_aware,gif_aware}.yaml
exp2_llama3_8b_tulu3__{vanilla,unaware,phase_aware,gif_aware}.yaml
```

## 2. 数据 manifest

同一任务只需生成一次；TL;DR 的两个模型共用 raw-sample manifest。

```bash
python code/scripts/prepare_data.py --config code/configs/generated/exp1_qwen3_1_7b_tldr__unaware.yaml
python code/scripts/prepare_data.py --config code/configs/generated/exp2_llama3_8b_tulu3__unaware.yaml
```

TL;DR 保存完整 train、完整 validation、test manifest 以及从 train 有放回抽取的128条 calibration manifest。Tulu 3 保存 seed42 无放回的100k train、其外1000条 validation，以及从100k train 有放回抽取的128条 calibration；抽中的 calibration 样本继续保留在训练集。

## 3. 每个模型准备 rotation、prefix 与两套 calibration

对每个 model-task pair，rotated 三种设置共用一套状态；Vanilla 单独生成原坐标状态。下面以 Qwen3-1.7B 为例：

```bash
ROT_CFG=code/configs/generated/exp1_qwen3_1_7b_tldr__unaware.yaml
VAN_CFG=code/configs/generated/exp1_qwen3_1_7b_tldr__vanilla.yaml

python code/scripts/prepare_rotation.py --config "$ROT_CFG"
python code/scripts/discover_prefix.py --config "$ROT_CFG"
python code/scripts/calibrate_sites.py --config "$ROT_CFG"
python code/scripts/calibrate_sites.py --config "$VAN_CFG"
```

把前缀替换为 `exp1_qwen3_8b_tldr` 和 `exp2_llama3_8b_tulu3`，分别执行同样四条命令。`prepare_rotation.py` 保存的是 fused Base checkpoint；后续微调直接优化其中的融合后权重。

如果 calibration/prefix discovery 需要2、3或4张卡做模型切分，可在单进程命令前限制相应 GPU，配置中的 `device_map: auto` 会使用所有可见设备：

```bash
CUDA_VISIBLE_DEVICES=0,1 python code/scripts/calibrate_sites.py --config "$ROT_CFG"
CUDA_VISIBLE_DEVICES=0,1,2 python code/scripts/calibrate_sites.py --config "$ROT_CFG"
CUDA_VISIBLE_DEVICES=0,1,2,3 python code/scripts/calibrate_sites.py --config "$ROT_CFG"
```

不要用多个进程同时写同一套 shared calibration。

## 4. 训练12个 ANN checkpoint

设置每个 model-task pair 固定使用的 GPU 数；同一 pair 的四种 ANN mode 必须一致。例如4卡：

```bash
NGPU=4
for MODE in vanilla unaware phase_aware gif_aware; do
  torchrun --standalone --nproc_per_node="$NGPU" \
    code/scripts/train_ann.py \
    --config "code/configs/generated/exp1_qwen3_1_7b_tldr__${MODE}.yaml"
done
```

对以下三个前缀分别运行：

```text
exp1_qwen3_1_7b_tldr
exp1_qwen3_8b_tldr
exp2_llama3_8b_tulu3
```

2卡或3卡只需把 `NGPU` 改为2或3。DeepSpeed ZeRO-3 由 `code/configs/deepspeed_zero3.json` 启用。有效 global batch 分别为32、48、64。每50个 optimizer step 验证并保存 validation-loss 最优 checkpoint。

## 5. ANN 参考评测

TL;DR 的8个 ANN checkpoint：

```bash
accelerate launch --num_processes 4 code/scripts/evaluate_tldr.py \
  --config code/configs/generated/exp1_qwen3_1_7b_tldr__vanilla.yaml \
  --neuron ann
```

将 config 依次替换为两个 TL;DR 模型的四种 mode。评测严格使用 test 前6528条、输入长度512、greedy `max_new_tokens=32`，并输出 ROUGE-1/2/L/Lsum。

Tulu 3 的4个 ANN checkpoint：

```bash
accelerate launch --num_processes 4 code/scripts/evaluate_lm_harness.py \
  --config code/configs/generated/exp2_llama3_8b_tulu3__vanilla.yaml \
  --neuron ann
```

将 mode 依次替换为四种设置。lm-eval 任务按照 config 中各自的 few-shot 数分别运行，不能用一个全局 `num_fewshot` 覆盖全部任务。

## 6. 创建36个 SNN conversion descriptor

对每个 ANN config 分别运行三种 neuron：

```bash
CFG=code/configs/generated/exp1_qwen3_1_7b_tldr__phase_aware.yaml
for NEURON in phase gif mtn; do
  python code/scripts/convert_snn.py --config "$CFG" --neuron "$NEURON"
done
```

对12个 config 全部执行，即得到 `12 × 3 = 36` 个 conversion descriptor。转换只加载微调前 frozen calibration state，不执行 post-fine-tuning recalibration。

## 7. 运行36次 SNN evaluation

TL;DR config：

```bash
CFG=code/configs/generated/exp1_qwen3_1_7b_tldr__phase_aware.yaml
for NEURON in phase gif mtn; do
  accelerate launch --num_processes 4 code/scripts/evaluate_tldr.py \
    --config "$CFG" --neuron "$NEURON"
done
```

Tulu 3 config：

```bash
CFG=code/configs/generated/exp2_llama3_8b_tulu3__phase_aware.yaml
for NEURON in phase gif mtn; do
  accelerate launch --num_processes 4 code/scripts/evaluate_lm_harness.py \
    --config "$CFG" --neuron "$NEURON"
done
```

对相应任务的全部 ANN mode 重复。Phase/MTN 的 full-temporal `T=4`；GIF 局部分解步数为 `2^a=2`。结果记录 model-forward 次数、temporal model-step forward 次数以及 `次数 × 层数 × 9 sites` 的 activation-site temporal operator 等价计数。

## 8. 校验工件

每个 config 完成 conversion/evaluation 后运行：

```bash
python code/scripts/verify_artifacts.py \
  --config code/configs/generated/exp1_qwen3_1_7b_tldr__phase_aware.yaml
```

校验内容包括：数据 manifest、ANN best checkpoint、每层九个 calibration site、三种 conversion metadata 和相应评测结果。

## 9. 日志与失败恢复

每一步都会生成：

```text
logs/<stage>[_rankN].log
logs/<stage>[_rankN].jsonl
logs/<stage>[_rankN]_result.json
```

result JSON 记录状态、开始/结束时间、wall-clock、rank/world size、每张可见 GPU 的峰值显存、GPU 型号和软件版本。训练恢复只允许显式设置生成 config 中的 `training.resume_from_checkpoint`；不得因 OOM 静默改变 micro-batch、gradient accumulation、eager attention 或 gradient checkpointing。
